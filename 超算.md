



#### 临时联网

```bash
export http_proxy=http://u-cEoRwn:EDvFuZTe@172.16.4.9:3128
export https_proxy=http://u-cEoRwn:EDvFuZTe@172.16.4.9:3128
```





###  进入计算节点

#### 1 交互式进入计算节点

如果你想临时进入一个计算节点，像开终端那样，可以用：

```
srun -p gpu --gres=gpu:1 --pty bash
```

进去后可检查：

```
hostname
nvidia-smi
```

------

#### 2 批处理提交到计算节点

如果你想正式跑程序，写好 `run.sh` 之后，用：

```
sbatch run.sh
```

这不是你手动进入，而是 Slurm 自动把脚本送到计算节点执行。

#### Slurm 作业脚本运行方式

如果是 Slurm 脚本，就用：

```
sbatch run.sh
```

不要简单地 `bash run.sh`，否则只是在当前节点直接跑。



### 可用的 `run.sh` 模板

下面这份是适合你当前场景的版本：

```bash
#!/bin/bash
#SBATCH -J clip_pretrain
#SBATCH -p gpu
#SBATCH --gres=gpu:1
#SBATCH -N 1
#SBATCH -n 1
#SBATCH --cpus-per-task=4
#SBATCH -t 24:00:00
#SBATCH -o /home/bingxing2/home/scx9951/Main_clip/logs/%x-%j.out
#SBATCH -e /home/bingxing2/home/scx9951/Main_clip/logs/%x-%j.err

module load miniforge3/24.1 compilers/cuda/11.1 compilers/gcc/9.3.0 cudnn/8.4.0.27_cuda11.x
source activate py39+torch1.11.0
export PYTHONUNBUFFERED=1

cd /home/bingxing2/home/scx9951/Main_clip || exit 1

echo "Job ID: $SLURM_JOB_ID"
echo "Node: $(hostname)"
echo "Start Time: $(date)"
echo "Workdir: $(pwd)"

nvidia-smi
python -m experiments.train_pretrain

echo "End Time: $(date)"
```

提交方式：

```
sbatch run.sh
```

### Slurm 提交之后怎么查看状态

#### 查看队列

假设作业号是 `1150311`：

```
squeue -j 1150311
```

或者看自己全部作业：

```
squeue -u $USER
```

状态常见含义：

- `PD`：排队中
- `R`：运行中
- `CG`：快结束了

------

#### 查看最终状态

如果作业已经不在队列里了，用：

```
sacct -j 1150311 --format=JobID,JobName,Partition,State,ExitCode,Elapsed,NodeList
```

常见状态：

- `COMPLETED`：正常结束
- `FAILED`：失败
- `CANCELLED`：取消
- `TIMEOUT`：超时
- `OUT_OF_MEMORY`：内存不足

------

#### 查看更详细的作业信息

```
scontrol show job 1150311
```

它可以显示：

- 工作目录
- 输出文件路径
- 错误文件路径
- 实际命令
- 节点名

------

### 日志文件的重要性

你脚本里写了：

```
#SBATCH -o /home/bingxing2/home/scx9951/Main_clip/logs/%x-%j.out
#SBATCH -e /home/bingxing2/home/scx9951/Main_clip/logs/%x-%j.err
```

所以日志一般在：

```
/home/bingxing2/home/scx9951/Main_clip/logs/
```

查看方式：

```bash
ls logs
tail -n 100 logs/clip_pretrain-作业号.out
tail -n 100 logs/clip_pretrain-作业号.err
```

------

## 

### 如何安装缺失依赖

先在登录节点执行：

```
module load miniforge3/24.1 compilers/cuda/11.1 compilers/gcc/9.3.0 cudnn/8.4.0.27_cuda11.x
source activate py39+torch1.11.0
```

然后安装：

```
pip install pytorch-lightning==1.8.6
```

#### 临时联网

```bash
export http_proxy=http://u-cEoRwn:EDvFuZTe@172.16.4.9:3128
export https_proxy=http://u-cEoRwn:EDvFuZTe@172.16.4.9:3128
```

安装完成后检查：

```
python -c "import pytorch_lightning as pl; print(pl.__version__)"
```

若能打印版本号，说明安装成功。

然后重新提交：

```
sbatch run.sh
```

------

### 为什么登录节点安装的包，计算节点也能用

这是因为你的平台大概率使用**共享文件系统**。

比如你的 conda 环境路径类似：

```
/home/bingxing2/home/scx9951/.conda/envs/py39+torch1.11.0
```

登录节点能看到它，计算节点也能看到它。

所以你在登录节点执行：

```
pip install pytorch-lightning
```

其实是把包装到了共享目录里。
 之后作业在计算节点激活同一个环境，自然也能用。

------

## 





