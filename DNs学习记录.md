# 神经网络结构中的数据先验

在机器学习中，模型通常可以表示为：
$$
y = f_\theta(x)
$$
其中，x 是输入数据，y$是输出结果，$\theta$ 是模型需要学习的参数，$f_\theta$ 表示带参数的神经网络模型。

**$$f_\theta$$ 的内部结构并不是随意设计的，而是由研究者根据数据特点进行设计，以尽可能融入关于数据的先验知识。**

------

#### 1. CNN：局部性和平移等变性先验

CNN 适合处理图像数据，因为图像通常具有明显的局部结构。

例如，图像中的边缘、纹理、眼睛、轮廓等特征，往往只出现在局部区域中。CNN 通过卷积核在局部窗口中提取特征，因此天然具有**局部性先验**。

同时，CNN 还具有**平移等变性**。也就是说，同一个物体或特征即使出现在图像的不同位置，卷积核仍然可以检测到它。

因此，CNN 融入的先验是：

> 图像中的局部模式很重要，并且相同模式可能出现在不同位置。

------

#### 2. ViT：全局关系建模先验

ViT 将图像划分为多个 patch，并将每个 patch 看作一个 token。

之后，ViT 通过 self-attention 建模不同 patch token 之间的关系。与 CNN 主要关注局部区域不同，ViT 可以直接建立图像中远距离区域之间的联系。

因此，ViT 融入的先验是：

> 图像可以被看作一组 patch token，不同区域之间可能存在全局依赖关系。

------

#### 3. Swin Transformer：局部窗口与层次结构先验

Swin Transformer 在 Transformer 的基础上引入了窗口注意力机制。

它不是让所有 patch 之间都进行全局 attention，而是在局部窗口内计算 attention，从而重新引入了局部性。同时，它还通过逐层合并 patch，形成类似 CNN 的多尺度层次结构。

因此，Swin Transformer 融入的先验是：

> 图像既具有局部结构，也具有从低级到高级的层次化特征。

------

#### 4. 图神经网络：图结构先验

图神经网络适合处理图结构数据，例如社交网络、分子结构、交通网络、有限元网格等。

这类数据的核心特点是：数据之间不是规则排列的，而是通过节点和边形成连接关系。

GNN 通过邻居节点之间的信息传递来更新节点特征。

因此，GNN 融入的先验是：

> 一个节点的状态会受到其邻居节点的影响，图中的连接关系非常重要。

------

#### 5. 时序 Transformer：时间依赖先验

时序 Transformer 适合处理序列数据，例如文本、语音、传感器信号、机器人轨迹和时间序列预测任务。

这类数据具有明确的时间顺序，当前状态通常与历史状态有关。Transformer 可以通过 attention 机制建模长时间范围内的依赖关系。

因此，时序 Transformer 融入的先验是：

> 数据具有时间顺序，当前状态与历史信息之间存在依赖关系。

------

#### 总结

不同神经网络结构本质上是在给模型加入不同类型的数据先验。研究者在设计 $f_\theta$ 的内部结构时，会根据数据的组织形式和任务特点选择合适的模型结构。

| 模型结构         | 适合的数据       | 融入的先验             |
| ---------------- | ---------------- | ---------------------- |
| CNN              | 图像             | 局部性、平移等变性     |
| ViT              | 图像、多模态数据 | patch token、全局关系  |
| Swin Transformer | 图像密集预测任务 | 局部窗口、层次结构     |
| GNN              | 图结构数据       | 节点邻接关系、消息传递 |
| 时序 Transformer | 时间序列数据     | 时间顺序、长程依赖     |

一句话总结：

> $f_\theta$ 不只是一个黑箱函数，它的结构设计本身就体现了研究者对数据规律的理解。不同模型结构对应不同的数据先验。