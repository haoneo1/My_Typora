# 超声UI界面设计





```python
def run_step0_demo() -> None:
    """运行 Step 0 的最小可视化示例。"""
    from PyQt6.QtCore import Qt
    from PyQt6.QtWidgets import QApplication, QLabel, QPushButton, QVBoxLayout, QWidget

    # 自定义 QLabel：重写鼠标按下事件，用于演示 Event 回调
    class ClickableLabel(QLabel):
        def mousePressEvent(self, event):
            # 只处理鼠标左键点击
            if event.button() == Qt.MouseButton.LeftButton:
                # 点击后修改标签文本
                self.setText("Event: 你点击了 Label")
            # 调用父类默认实现，保持 QLabel 原本行为
            super().mousePressEvent(event)

    # Qt 应用对象（每个 Qt 程序都需要一个 QApplication）
    app = QApplication([])
    # 主窗口（这里用最基础 QWidget）
    window = QWidget()
    window.setWindowTitle("Step0 Demo")
    # 垂直布局：控件会从上到下排列
    layout = QVBoxLayout(window)

    # 标签控件：用于显示文本
    label = ClickableLabel("QLabel(Widget): 点击我")
    # 按钮控件：点击后触发 clicked 信号
    button = QPushButton("QPushButton(Widget): 点击我")

    # Signal/Slot：按钮 clicked 信号 -> 调用 setText 更新 label
    button.clicked.connect(lambda: label.setText("Signal/Slot: clicked -> setText"))

    # 把控件加入布局
    layout.addWidget(label)
    layout.addWidget(button)
    # 设置窗口初始大小并显示
    window.resize(420, 140)
    window.show()
    # 进入事件循环（程序会阻塞在这里，直到窗口关闭）
    app.exec()
```

- `ClickableLabel(QLabel)`：通过重写 `mousePressEvent` 演示 Event 回调机制（左键点击后改文本）。
- `super().mousePressEvent(event)`：保留父类默认行为，避免自定义后破坏原控件逻辑。
- `app = QApplication([])`：Qt 程序入口对象，负责管理 GUI 应用生命周期。
- `window = QWidget()`：最基础窗口容器，示例中不用 `QMainWindow`，降低理解门槛。
- `layout = QVBoxLayout(window)`：垂直布局管理器，控件按“上到下”自动排布。
- `label` 和 `button`：最小 Widget 组合，分别承载“显示状态”和“触发动作”。
- `button.clicked.connect(...)`：Signal/Slot 核心写法，按钮点击信号绑定到更新文本动作。
- `window.show()`：让窗口可见；没有这句窗口不会显示。
- `app.exec()`：进入事件循环，程序在这里等待点击、键盘、重绘等事件。







# UI

## 主要class

- `DrawableVideoLabel`：视频显示 + 框选区域。负责接收鼠标按下/移动/释放事件，把用户画出的矩形记录下来并在画面上绘制。
- `TitleBar`：悬浮窗标题栏 + 拖动入口。负责显示标题文本，并在完整版本里处理拖动逻辑（拖动整个悬浮窗）。
- `ResizableOverlay`：视频悬浮容器。把 `TitleBar` 和 `DrawableVideoLabel` 组合在一起；在完整版本里还负责缩放、透明度切换、边界限制。
- `MainWindow`：总控窗口。负责搭建整体布局、创建上面几个组件、绑定按钮事件、管理模式切换（Select/Record/Inference）和视频线程



# 拷贝数据

```
rsync -avhP --append-verify baomingkun@dual-workstation1:/extend/baomingkun/data/navi_neo/ /home/mig/Documents/navi_neo/
```



# Bao代码

```
CUDA_VISIBLE_DEVICES=1  uv run launch.py runners/run_simple_train.py -g config_hao -n Aug_5 --local -- configs/config_hao/person_resnet18_gray_edge3_NextAug.yaml

```









```
scp -P 22 -r root@10.108.0.126:/media/egoexo4d/annotations/ego_pose ~/Documents/
```





































